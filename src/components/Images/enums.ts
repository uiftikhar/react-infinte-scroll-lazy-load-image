export enum ImagesTypes {
  Stack = 'STACK_IMAGES',
  Fetching = 'FETCHING_IMAGES',
  Error = 'ERROR',
  AddFavorite = 'ADD_FAVORITE',
  RemoveFavorite = 'REMOVE_FAVORITE',
  GetUser = 'GET_USER',
}

export enum PagerTypes {
  AdvancePage = 'ADVANCE_PAGE',
}
export enum LocalStorage {
  Favorites = 'Favorites',
}
